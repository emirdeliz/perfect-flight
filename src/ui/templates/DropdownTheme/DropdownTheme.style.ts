import styled from 'styled-components';

const MAX_WIDTH = '200px';

export const DropdownTheme = styled.div`
  max-width: ${MAX_WIDTH};
  width: 100%;
  position: absolute;
  top: 15px;
  z-index: 9999;
`;
